# 書籍管理ドメイン - 機能設計書

プロジェクトID: back-office-api  
ドメイン: books  
バージョン: 1.0.0  
最終更新日: 2026-02-05

---

## 1. 概要

本ドキュメントは、書籍管理ドメインの機能を定義する。

---

## 2. 機能一覧

### 2.1 書籍管理機能

| 機能ID | 機能名 | 説明 |
|--------|--------|------|
| F-BOOK-001 | 書籍一覧取得 | すべての書籍情報を取得 |
| F-BOOK-002 | 書籍詳細取得 | 指定した書籍IDの詳細情報を取得 |
| F-BOOK-003 | 書籍検索（JPQL） | カテゴリとキーワードで書籍を検索（静的クエリ） |
| F-BOOK-004 | 書籍検索（Criteria API） | カテゴリとキーワードで書籍を検索（動的クエリ） |
| F-BOOK-005 | カテゴリ一覧取得 | 書籍カテゴリのマップ形式一覧を取得 |

---

## 3. API詳細設計

### 3.1 F-BOOK-001: 書籍一覧取得

#### 3.1.1 機能概要

すべての書籍情報（在庫情報含む）を取得する。

#### 3.1.2 エンドポイント

* メソッド: GET
* パス: `/api/books`
* 認証: 必要（JWT）

#### 3.1.3 入力

なし

#### 3.1.4 処理フロー

1. BookServiceを呼び出し
2. BookDaoで全書籍を取得（JPQL）
3. BookエンティティをBookTOに変換
   * カテゴリ情報（CategoryInfo）
   * 出版社情報（PublisherInfo）
   * 在庫情報（quantity, version）
4. リスト形式でレスポンス生成

#### 3.1.5 出力

* 成功（200 OK）: BookTOのリスト

#### 3.1.6 関連コンポーネント

* 書籍リソース（書籍一覧取得）
* 書籍サービス（ビジネスロジック）
* 書籍データアクセス（全件取得）

---

### 3.2 F-BOOK-002: 書籍詳細取得

#### 3.2.1 機能概要

指定した書籍IDの詳細情報を取得する。

#### 3.2.2 エンドポイント

* メソッド: GET
* パス: `/api/books/{bookId}`
* 認証: 必要（JWT）

#### 3.2.3 入力

* 書籍ID（bookId）: Integer

#### 3.2.4 処理フロー

1. パスパラメータから書籍IDを取得
2. BookServiceを呼び出し
3. BookDaoで指定IDの書籍を取得
4. 書籍が存在しない場合 → 404 Not Found
5. BookエンティティをBookTOに変換
6. レスポンス生成

#### 3.2.5 出力

* 成功（200 OK）: BookTO
* 失敗（404 Not Found）: ErrorResponse

#### 3.2.6 関連コンポーネント

* 書籍リソース（書籍詳細取得）
* 書籍サービス（ビジネスロジック）
* 書籍データアクセス（ID検索）

---

### 3.3 F-BOOK-003: 書籍検索（JPQL）

#### 3.3.1 機能概要

カテゴリIDとキーワードで書籍を検索する（静的クエリ使用）。

#### 3.3.2 エンドポイント

* メソッド: GET
* パス: `/api/books/search/jpql`
* 認証: 必要（JWT）

#### 3.3.3 入力

* カテゴリID（categoryId）: Integer（オプション）
* キーワード（keyword）: String（オプション）

#### 3.3.4 処理フロー

1. クエリパラメータから検索条件を取得
2. 検索条件に応じてBookServiceのメソッドを呼び分け
   * カテゴリID + キーワード → `searchBook(categoryId, keyword)`
   * カテゴリIDのみ → `searchBook(categoryId)`
   * キーワードのみ → `searchBook(keyword)`
   * 条件なし → `getBooksAll()`
3. BookDaoでJPQLクエリ実行
4. BookエンティティをBookTOに変換
5. リスト形式でレスポンス生成

#### 3.3.5 出力

* 成功（200 OK）: BookTOのリスト

#### 3.3.6 関連コンポーネント

* 書籍リソース（書籍検索）
* 書籍サービス（検索ロジック）
* 書籍データアクセス（動的検索）

---

### 3.4 F-BOOK-004: 書籍検索（Criteria API）

#### 3.4.1 機能概要

カテゴリIDとキーワードで書籍を検索する（Criteria API使用、動的クエリ）。

#### 3.4.2 エンドポイント

* メソッド: GET
* パス: `/api/books/search/criteria`
* 認証: 必要（JWT）

#### 3.4.3 入力

* カテゴリID（categoryId）: Integer（オプション）
* キーワード（keyword）: String（オプション）

#### 3.4.4 処理フロー

1. クエリパラメータから検索条件を取得
2. BookServiceを呼び出し
3. BookDaoでCriteria APIクエリ実行
4. BookエンティティをBookTOに変換
5. リスト形式でレスポンス生成

#### 3.4.5 出力

* 成功（200 OK）: BookTOのリスト

---

### 3.5 F-BOOK-005: カテゴリ一覧取得

#### 3.5.1 機能概要

書籍カテゴリのマップ形式一覧を取得する。

#### 3.5.2 エンドポイント

* メソッド: GET
* パス: `/api/books/categories`
* 認証: 必要（JWT）

#### 3.5.3 入力

なし

#### 3.5.4 処理フロー

1. BookServiceを呼び出し
2. CategoryDaoで全カテゴリを取得
3. Map<CategoryId, CategoryName>形式に変換
4. レスポンス生成

#### 3.5.5 出力

* 成功（200 OK）: Map<Integer, String>

---

## 4. ビジネスルール

### 4.1 書籍管理ルール

#### BR-BOOK-001: 論理削除

* 書籍の削除は論理削除（DELETEDフラグ）で実施
* 論理削除された書籍はAPIレスポンス（一覧・検索）から除外される
* 書籍詳細取得APIでは論理削除された書籍も参照可能
* 物理削除は実施しない（データ保持・履歴保持のため）

#### BR-BOOK-002: 新規書籍の初期在庫

* ワークフロー承認時の新規書籍追加では在庫数を0で作成
* 在庫数は別途在庫更新APIで変更

---

## 5. パフォーマンス考慮事項

### 5.1 N+1問題の回避

* 書籍エンティティは在庫と結合して取得
* 関連エンティティは必要に応じて結合取得

### 5.2 インデックスの活用

* 検索条件に使用されるカラムにインデックスを作成
* 外部キーカラムにもインデックスを作成

---

## 6. 参考資料

* [../common/architecture_design.md](../common/architecture_design.md) - アーキテクチャ設計書
* [../common/data_model.md](../common/data_model.md) - データモデル仕様書
* [behaviors.md](behaviors.md) - 書籍管理ドメインの振る舞い仕様書（結合テスト用）
