# [DOMAIN_NAME] - ドメイン詳細設計書

ドメイン名: [DOMAIN_NAME]  
バージョン: 1.0.0  
最終更新: [DATE]

---

## 重要な原則

この詳細設計書は、基本設計（what/why）と実装コード（how）の橋渡しとなる設計判断のみを簡潔に記載します。

記載すべき情報:
- クラス名と責務（1行）
- 主要メソッドのシグネチャ（引数、戻り値、例外）
- 設計判断を示すアノテーション（@Transactional, @Path等）
- JPQLクエリ（WHERE句、JOIN等の設計判断）
- 依存関係（@Inject対象）

記載すべきでない情報:
- メソッドの実装詳細、処理ステップ
- すべてのフィールド定義、getter/setter
- バリデーションの詳細
- 基本設計SPECの内容の繰り返し

---

## 1. ドメイン概要

* ドメイン名: [DOMAIN_NAME]
* 責務: [1行で記載]
* 依存関係: [commonに依存、または他のドメインに依存]

---

## 2. クラス構成

### 2.1 パッケージ構造

```
[BASE_PACKAGE]
├── api/                    # JAX-RS Resources（該当する場合）
│   ├── [Resource名].java
│   └── dto/
│       ├── [Request名].java
│       └── [Response名].java
├── service/                # ビジネスロジック（該当する場合）
│   └── [Service名].java
├── dao/                    # データアクセス（該当する場合、commonドメイン）
│   └── [Dao名].java
├── entity/                 # JPAエンティティ（該当する場合、commonドメイン）
│   └── [Entity名].java
├── security/               # セキュリティ（該当する場合、commonドメイン）
│   └── [Component名].java
└── external/               # 外部API連携（該当する場合）
    ├── [RestClient名].java
    └── dto/
        └── [外部API用DTO名].java
```

---

## 3. コンポーネント設計

### 3.1 [コンポーネント種別] - [クラス名]

責務: [1行で記載]

アノテーション:
- `@[アノテーション名]` - [設計判断を示すもののみ記載]

依存関係:
- `@Inject [型] [変数名]` - [依存先の説明]

主要メソッド:

#### [メソッド名]

```java
public [戻り値型] [メソッド名]([引数]) throws [例外]
```

- 目的: [1行で記載]
- JPQLクエリ (該当する場合):
  ```sql
  [WHERE句、JOIN等の設計判断のみ]
  ```

---

### 3.2 [コンポーネント種別] - [クラス名2]

[同様の形式で記載]

---

## 4. DTO設計（該当する場合）

### 4.1 [DTO名]

目的: [リクエスト/レスポンス/外部API連携]

フィールド:
- `[型] [フィールド名]` - [説明]
- `[型] [フィールド名]` - [説明]

バリデーション (重要なもののみ):
- `@NotBlank` on [フィールド名]
- `@Min([値])` on [フィールド名]

---

## 5. エンティティ設計（commonドメインの場合）

### 5.1 [Entity名]

テーブル名: `[テーブル名]`

主要フィールド:
- `[型] [フィールド名]` - `@Column(name="[カラム名]")`

リレーション:
- `@ManyToOne` - [関連エンティティ]
- `@OneToMany` - [関連エンティティ]

アノテーション:
- `@Entity`
- `@Table(name = "[テーブル名]")`
- `@Version` (楽観的ロック、該当する場合)

---

## 6. 外部API連携（該当する場合）

### 6.1 [RestClient名]

連携先: [外部API名]

主要メソッド:

#### [メソッド名]

```java
public [戻り値型] [メソッド名]([引数])
```

- エンドポイント: `[HTTPメソッド] [パス]`
- タイムアウト: [設定値]（該当する場合）

---

## 7. トランザクション設計（該当する場合）

- トランザクション境界: [Service層/Resource層]
- 伝播レベル: [REQUIRED/REQUIRES_NEW等]（デフォルト以外の場合のみ記載）

---

## 8. 参考資料

* [behaviors.md](behaviors.md) - 単体テスト用振る舞い仕様書
* `../../basic_design/[DOMAIN_NAME]/functional_design.md` - ドメイン機能設計書
* `../../basic_design/common/data_model.md` - データモデル仕様書（該当する場合）
